{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "The shared renderer may call only FetchMoji's two explicit Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
